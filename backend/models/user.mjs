class User {
    username;
    password;
    tasks;

    constructor(username, password, tasks) {
        this.username = username;
        this.password = password;
        this.tasks = tasks;
    }
}

export default User